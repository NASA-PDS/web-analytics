# IAM-module prod values — module-specific only.
# Shared infra values (partition, s3_bucket_prefix, etc.) are in common-infra-prod.tfvars.
# Shared global values (aws_region, tenant, etc.) are in common.tfvars.

venue     = "prod"
managedby = "pdsoperator@jpl.nasa.gov"
