# IAM-module dev values — module-specific only.
# Shared infra values (partition, s3_bucket_prefix, etc.) are in common-infra-dev.tfvars.
# Shared global values (aws_region, tenant, etc.) are in common.tfvars.

venue     = "dev"
managedby = "your-email@jpl.nasa.gov"
